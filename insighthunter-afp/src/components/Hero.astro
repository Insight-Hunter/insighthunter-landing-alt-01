<section class="hero glass glow-purple">
  <h1>Insight Hunter</h1>
  <p>AI-powered intelligence for modern decision-makers.</p>
  <button>Get Early Access</button>
</section>

<style>
.hero {
  max-width: 720px;
  margin: 15vh auto;
  padding: 3rem;
  text-align: center;
}

h1 {
  font-size: clamp(2.5rem, 5vw, 3.5rem);
  background: linear-gradient(
    90deg,
    hsl(var(--purple)),
    hsl(var(--cyan))
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

button {
  margin-top: 2rem;
  padding: 0.75rem 2rem;
  border-radius: 999px;
  border: none;
  background: linear-gradient(
    90deg,
    hsl(var(--purple)),
    hsl(var(--cyan))
  );
  color: black;
  font-weight: 600;
  cursor: pointer;
}
</style>
